//==============================================================================
// ■ Utilities (utils.js)
//------------------------------------------------------------------------------
//     General purposes utility functions.
//==============================================================================
// export default {
//   sortArrayByDate(array, dateFieldName) {
//     const newDate = item => new Date(item[dateFieldName]);
//     return [...array].sort((a, b) => newDate(a) - newDate(b));
//   },
//   stringToArray(text, divider = ",", trim = true) {
//     let arr = text.split(divider);
//     return trim ? arr.map(item => item.trim()) : arr;
//   },
//   shuffleArray(array) {
//     let resultArray = [...array];
//     for (let i = resultArray.length - 1; i > 0; i--) {
//       const j = Math.floor(Math.random() * (i + 1));
//       [resultArray[i], resultArray[j]] = [resultArray[j], resultArray[i]];
//     }
//     return resultArray;
//   }
// };
